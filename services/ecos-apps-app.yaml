version: '2.4'
services:
  app:
    logging:
      options:
        max-size: "10m"
        max-file: "5"
    hostname: ecos-apps-app
    restart: always
    stop_grace_period: 1m
    image: nexus.citeck.ru/ecos-apps:2.5.1
    expose:
      - 8089/tcp
    env_file:
      - ./environments/ecos-apps-app.env
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8089/management/health"]
      interval: 20s
      timeout: 20s
      retries: 36
    networks:
      - app_network
